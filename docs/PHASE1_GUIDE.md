# Switch 2 Pro Controller – フェーズ1 ガイド

このドキュメントは、`switch2_procon_init_test.py` を実行した後の「受信結果パターン別」の対処法と、パケットを解析する際のコツをまとめたものです。

---

## 1. 実行前の準備チェックリスト

1. **USB接続確認** – 有線でPCに接続し、デバイスマネージャーに認識されていることを確認。  
2. **他アプリの終了** – Steam 入力、BetterJoy、DS4Windows、HidHide など、コントローラーを占有する可能性のあるアプリはすべて終了。  
3. **ドライバー（Zadig）** – Nintendo製コントローラーは通常、複数のHIDインターフェースを持ちます。  
   うち1つは Windows が標準HID（ジョイスティック）として確保します。  
   もう1つの **ベンダー固有インターフェース** を `hidapi` で開くには、**WinUSB** または **libusbK** ドライバーが必要な場合があります。  
   [Zadig](https://zadig.akeo.ie) で対象デバイスの `Interface 1`（UsagePage が `0xFF00` 等のもの）を WinUSB に置き換えてください。  
4. **管理者権限** – Windows ではドライバー周りの制限で失敗する場合があるため、必要に応じて **PowerShell / コマンドプロンプトを管理者として実行** してください。

---

## 2. 結果パターン別の対処法

### Pattern A：データ（HEXダンプ）が流れてきた場合 ✅

```
RECV [64]: 30 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 00 ... <-- CHANGE
```

この状態であれば、**有線接続の初期化は成功**です。次のフェーズ（パース・マッピング）に進めます。

#### 即座にやること
1. **静止状態（何も押さない）のダンプをコピー保存**します。  
   これが「ベースライン」になります。
2. **1つのボタンだけ押して、どのバイト・ビットが変化するか**を観察します。  
   （後述の「解析コツ」を参照）
3. **変更フラグ (`<-- CHANGE`) が出るたびに対応関係をメモ**します。

---

### Pattern B：接続はされるがデータが流れてこない（初期化失敗）❌

`RECV` が一切表示されず、`.` だけが続く、またはエラーで終了する場合は以下を順に確認してください。

#### チェックポイント1：インターフェース番号
Nintendo Pro Controller 系は USB 上で **2つ以上の HID インターフェース** を持ちます。  
スクリプトは自動的に全インターフェースを順番に開きますが、**開けたけど書き込み権限がない** 場合は初期化パケットが送出できず無反応になります。

- Zadig で **Interface 1**（または Interface 0 以外）に WinUSB を適用してください。  
- 複数のインターフェースが表示された場合、UsagePage/Usage が `0x0001 / 0x0004` や `0x0001 / 0x0008` ではなく、`0xFF00` などの **ベンダー固有** の方が該当です。

#### チェックポイント2：初期化コマンドの変更
スクリプトに書かれたシーケンス（`0x80 0x01` および ReportMode 設定）が、Switch 2 Pro Controller では別の形式かもしれません。

以下を順に試行してください。

1. **Handshake バリエーション**  
   `switch2_procon_init_test.py` の `attempt_initialization()` 内の `candidates` を以下のように差し替えて試す：  
   - `b"\x80\x02"`  
   - `b"\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00"`（全部ゼロの 64 バイト）  
   - 初代プロコンでは不要だが、一部クローン/新型では `0x80` ではなく Feature Report として送る必要がある場合もあるため、`device.send_feature_report(b"\x80\x01")` を試す。

2. **Feature Report 送信の試行**  
   `device.write(...)` の代わりに `device.send_feature_report(...)` を使う。  
   （`hid` ライブラリの場合）

3. **Set Report Mode のパラメータ変更**  
   現在 `0x3F`（Simple）と `0x30`（Standard Full）を試していますが、Switch 2 固有のモード番号（例: `0x21`, `0x31` 等）がある可能性があります。  
   `build_output_report_0x01(0x03, bytes([0x3F]))` の `0x3F` を `0x00`, `0x21`, `0x31`, `0x79` などに変えてみてください。

#### チェックポイント3：占有・権限・タイムアウト
- **他アプリの占有** – タスクマネージャーで `steam.exe`, `steamwebhelper.exe`, `betterjoy.exe` 等がないか確認。  
- **タイムアウト** – コントローラーは初期化後に数秒のラグを置いて初めてデータを流すことがあります。スクリプトの `READ_TIMEOUT_MS` を `10000` (10秒) に増やしてみる。  
- **管理者権限** – PowerShell を右クリック →「管理者として実行」してから `python switch2_procon_init_test.py` を実行。

---

## 3. パケット解析のコツ（どのボタンがどこか見分ける方法）

### 3.1 ボタン・デジタル入力（ビット単位）

ボタンは通常、**1 バイト内の特定ビット**が `0` → `1`（または `1` → `0`）にトグルします。

**手順：**
1. コントローラーを動かさず、何も押さない状態のダンプを記録（ベース）。
2. **A ボタンだけ**を押してダンプを記録。
3. 2つのダンプを比較。変化したバイトを特定します。  
   例えば Byte 3 が `0x00` → `0x08` に変わった場合、**Byte 3 の bit 3** に A ボタンが対応している可能性が高いです。

**初代 Pro Controller での参考例（Switch 2 でも構造は類似すると予想）：**

| 入力 | よくあるバイト位置 | 備考 |
|------|-------------------|------|
| Y, X, B, A | Byte 3 | 各 bit が対応（順序はデバイス依存） |
| SR, SL, R, ZR | Byte 3 or 4 | 右肩ボタン群 |
| Minus, Plus, R-Stick, L-Stick | Byte 4 | 中央系ボタン |
| Home, Capture | Byte 4 or 5 | 小さいボタン |
| Down, Up, Right, Left | Byte 5 | 十字キー |
| SR, SL, L, ZL | Byte 5 or 6 | 左肩ボタン群 |

> ⚠️ 上記は **初代 Pro Controller の参考値** です。Switch 2 Pro Controller ではビット位置や順序が変わっている可能性があるため、**必ず自分のダンプで確認してください**。

### 3.2 アナログスティック（連続値・マルチバイト）

スティックの値は **12 ビット（0–4095）** であることが多く、**ビッグエンディアンまたはリトルエンディアンで複数バイトに分割**されています。

**見分け方：**
- スティックを**左端まで倒す** → 特定のバイト群が最小値（`0x00` 付近）に落ち着く。
- **右端まで倒す** → 同じバイト群が最大値（`0xFF` や `0x0F`）に近づく。
- **中央** → 中間値（`0x80` 付近）に戻る。

**コツ：**
- 左スティック X, 左スティック Y, 右スティック X, 右スティック Y の順で **1 軸ずつゆっくり動かす**と、変化するバイトのブロックが明確に見えます。
- 変化量が小さい（上位ニブルだけ変わる等）は **12 ビット packed データ**の可能性。  
  例: `0x0F 0xF0` のような並びで、4 ビットずつ重なっているケースがあります。

### 3.3 アナログトリガー（ZL / ZR）

Switch 2 Pro Controller は **アナログトリガー（ホール効果等）** を採用している可能性があります。

**見分け方：**
- ZL を **ゆっくり引く**と、特定のバイトが `0x00` から `0xFF` まで**連続的・単調に増加**します。
- 離すと `0x00` に戻る。
- ZR も同様に別のバイト（または隣接バイト）で同じ挙動を示すはずです。

**参考：** もしアナログトリガーが存在しない（純粋なデジタル）場合、トリガーは「ボタン（ビット）」として扱われます。

### 3.4 効率の良い解析ワークフロー

1. **静止ベースライン**をファイル（`idle.txt`）に保存。  
2. **各ボタンを1つずつ押した状態**を `button_a.txt`, `button_b.txt` ... に保存。  
3. VS Code や WinMerge で `idle.txt` と各ファイルを比較。  
4. 変化したバイト位置・ビットをエクセル/テーブルに記録。  
5. アナログ（スティック・トリガー）については、**最小値・中央値・最大値**の 3 パターンを記録し、スケーリング式（`value / 4095 * 32767` 等）を導出します。

---

## 4. 次のフェーズへの橋渡し

解析が完了し、以下が分かった時点で **フェーズ2（vgamepad へのマッピング実装）** に進みます。

- [ ] 各ボタンのビットマスクが判明している  
- [ ] 左スティック X/Y のバイト位置とスケーリング式が判明している  
- [ ] 右スティック X/Y のバイト位置とスケーリング式が判明している  
- [ ] ZL/ZR のバイト位置（デジタル bit or アナログ byte）が判明している  

フェーズ2 では、上記のパース結果をリアルタイムに `vgamepad`（仮想 Xbox 360 パッド）へ反映させるメインループを実装します。
